import { ArrayCombineSubstringPipe } from './array-combine-substring.pipe';

describe('SubstringPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayCombineSubstringPipe();
    expect(pipe).toBeTruthy();
  });
});
